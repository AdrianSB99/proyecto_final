<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Resultados de búsqueda</title>
    <!-- Agrega aquí tus estilos CSS personalizados -->
    <style>
        /* Estilos generales */
        body {
            font-family: Arial, sans-serif;
            background-color: #f2f2f2;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        h1 {
            text-align: center;
            margin-top: 30px;
        }

        form {
            text-align: center;
            margin-bottom: 20px;
        }

        form input[type="text"] {
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
            resize: auto;
        }

        form button[type="submit"] {
            padding: 10px;
            background-color: #4CAF50;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        .cuerpo {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            grid-gap: 20px;
            margin-top: 20px;
        }

        .card {
            background-color: #fff;
            border-radius: 4px;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
            display: flex;
            align-items: center;
            padding: 20px;
        }

        .card img {
            width: 100px; /* Ancho fijo para la imagen */
            height: 100px; /* Altura fija para la imagen */
            object-fit: cover;
            border-radius: 50%; /* Forma cuadrada */
            margin-right: 20px;
        }

        .card .contenido {
            flex: 1;
        }

        .card h3 {
            margin-top: 0;
        }

        .card p {
            margin-bottom: 10px;
        }

        .volver-inicio {
            text-align: center;
            margin-top: 20px;
        }

        .volver-inicio button {
            padding: 10px 20px;
            background-color: #ccc;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        .volver-inicio button a {
            text-decoration: none;
            color: #fff;
        }

        .volver-inicio button:hover {
            background-color: #999;
        }
    </style>
</head>
<body>
    <div class="container">
        <form action="/buscaralumnos" method="post">
            <input type="text" name="nombre" placeholder="Nombre del alumno">
            <button type="submit">Buscar</button>
        </form>

        <h1>Resultados de búsqueda</h1>

        <div class="cuerpo">
            <% rows.forEach(tblalumnos => { %>
            <div class="card">
                <img src="/public/imagenes/<%= tblalumnos.foto %>" alt="Imagen de <%= tblalumnos.nombre %>">
                <div class="contenido">
                    <h3><%= tblalumnos.nombre %></h3>
                    <p>No. de control: <%= tblalumnos.nocontrol %></p>
                    <p>Semestre: <%= tblalumnos.semestre %></p>
                    <p>Contacto: <%= tblalumnos.correo %></p>
                    <p><%= tblalumnos.descripcion %></p>
                </div>
            </div>
            <% }) %>
        </div>

        <div class="volver-inicio">
            <button type="button"><a href="/BinicioAlu">Volver a inicio</a></button>
        </div>
    </div>
</body>
</html>